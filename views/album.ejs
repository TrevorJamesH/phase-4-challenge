<!DOCTYPE html>
<html>
<head>
  <% include header.ejs%>
  <title>Vinyl : <%= album.title %></title>
  <link rel="stylesheet" type="text/css" href="/style.css">
  <script type="text/javascript" src="/script.js"></script>
</head>
<body>

  <h1><%= album.title %></h1>
  <em>by <%= album.artist %></em>

  <h2>Reviews</h2>
  <% if( login ) { %>
    <button onClick = 'review( <%= album.id %> )'>
      Add Review
    </button>
  <% } %>
  <br>

  <% reviews.forEach(review => { %>

    <br>
    <%= review.text %>
    <% if( login && review.user_id === user.id ){ %>
      <button onClick = 'deleteReview( <%= review.id %> )'>	🗑 </button>
      <% } %>
    <br>
    reviewed by
    <a href='/profile/<%= review.user_id %>'>
    <%= review.name%>
    </a>
    <br>
    <br>
  <% }) %>

</body>
</html>
